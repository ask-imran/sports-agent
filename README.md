# Sports Agent

## Mock API Server

Start the mock sports data API server:

```bash
./scripts.sh start-mock-api
```

The server will run on `http://localhost:8000` with the following endpoints:
- `/teams` - Get all teams
- `/players` - Get all players  
- `/matches` - Get all matches
- `/docs` - Interactive API documentation